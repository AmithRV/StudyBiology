<div class="bg-[#004BB8] text-white text-sm">
  <div
    class="max-w-full px-4 md:px-8 py-2 flex flex-col md:flex-row items-center justify-between gap-2 md:text-left"
  >
    <div class="flex items-center justify-center md:justify-start gap-3">
      <span>Follow us on:</span>
      <a href="#"><i class="fab fa-instagram hover:text-[#FF9010]"></i></a>
      <a href="#"><i class="fab fa-facebook-f hover:text-[#FF9010]"></i></a>
      <a href="#"><i class="fab fa-youtube hover:text-[#FF9010]"></i></a>
    </div>
    <div class="flex flex-row gap-5 sm:gap-6 text-xs">
      <span class="flex items-center gap-1 justify-center"
        ><i class="fas fa-phone"></i> +91 9876543210</span
      >
      <span class="flex items-center gap-1 justify-center"
        ><i class="fas fa-envelope"></i> bio@study.com</span
      >
    </div>
  </div>
</div>

<!-- ✅ Navbar -->
<nav class="bg-white shadow relative z-30 sticky top-0">
  <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
    <!-- Logo -->
    <div class="text-2xl font-extrabold text-[#004BB8]">
      <a href="/">StudyBiology</a>
    </div>

    <!-- Desktop Menu -->
    <div class="hidden md:flex space-x-6 text-gray-700 font-medium">
      <a href="#" class="hover:text-[#004BB8]">Home</a>
      <a href="#" class="hover:text-[#004BB8]">About</a>
      <a href="#" class="hover:text-[#004BB8]">Course</a>
    </div>

    <!-- Hamburger Icon -->
    <div class="md:hidden">
      <button id="menuBtn" class="text-2xl text-gray-700 focus:outline-none">
        <i class="fas fa-bars"></i>
      </button>
    </div>
  </div>
</nav>

<div
  id="sidebar"
  class="fixed top-0 right-0 w-64 h-full bg-white shadow-lg z-50 transform translate-x-full transition-transform duration-300 ease-in-out md:hidden"
>
  <div class="flex justify-between items-center p-4 border-b">
    <div class="text-xl font-bold text-[#004BB8]">Menu</div>
    <button id="closeBtn" class="text-xl text-gray-700">
      <i class="fas fa-times"></i>
    </button>
  </div>
  <nav class="flex flex-col p-4 text-gray-700 font-medium space-y-4">
    <a href="#" class="hover:text-[#004BB8]">Home</a>
    <a href="#" class="hover:text-[#004BB8]">About</a>
    <a href="#" class="hover:text-[#004BB8]">Course</a>
  </nav>
</div>

<div id="overlay" class="fixed inset-0 bg-black/50 hidden z-40 md:hidden"></div>

<!-- 🧪 Hero Section -->
<section class="relative w-full py-28 md:py-40 overflow-hidden">
  <!-- Background Carousel -->
  <div class="absolute inset-0 z-0 carousel-container">
    <!-- Slide 1 -->
    <div
      class="absolute carousel-slide active inset-0 opacity-100 transition-opacity duration-[1500ms] ease-in-out bg-cover bg-right"
      style="background-image: url('https://jthemes.net/themes/html/etreeks/files/images/slider/slide-1.jpg');"
    >
    </div>

    <!-- Slide 2 -->
    <div
      class="absolute carousel-slide inset-0 opacity-0 transition-opacity duration-[1500ms] ease-in-out bg-cover bg-right"
      style="background-image: url('https://wallpaperbat.com/img/560382-wallpaper-girl-blue-beautiful-studying-student-image-for-desktop-section-diewushki.jpg');"
    >
    </div>

    <!-- Slide 3 -->
    <div
      class="absolute carousel-slide inset-0 opacity-0 transition-opacity duration-[1500ms] ease-in-out bg-cover bg-center"
      style="background-image: url('https://img.freepik.com/free-photo/pleased-showing-thumbs-up-young-female-teacher-sitting-table-with-school-tools-classroom_141793-114419.jpg');"
    >
    </div>
  </div>

  <!-- Gradient Overlay -->
  <div
    class="absolute inset-0 bg-gradient-to-r from-[#004BB8]/50 to-transparent z-10"
  >
  </div>

  <!-- Hero Content -->
  <div class="relative z-20 h-full px-6 md:px-20">
    <div class="text-white max-w-2xl">
      <h1
        class="text-3xl md:text-5xl font-extrabold leading-tight tracking-tight hero-title"
      >
        25K+ STUDENTS TRUST OUR<br />
        <span class="text-[#FE3E57]">BIOLOGY</span> ONLINE COURSES
      </h1>
      <p
        class="mt-5 text-white/90 text-base md:text-xl leading-relaxed font-medium hero-subtitle"
      >
        Feugiat primis ligula gravida auctor egestas augue viverra mauri tortor
        in iaculis placerat an egestas mauris ipsum undo viverra.
      </p>
      <div class="mt-8 flex flex-wrap gap-6 hero-buttons">
        <button
          class="bg-[#FE3E57] hover:bg-[#e43047] text-white font-semiboldtext-sm md:text-base py-3 md:px-8 px-4 rounded-lg shadow-md transform hover:scale-105 transition-all duration-300"
        >
          View Popular Courses
        </button>
        <button
          class="bg-white text-[#004BB8] hover:bg-blue-50 font-semiboldtext-sm md:text-base py-3 md:px-8 px-4 rounded-lg shadow-md flex items-center gap-3 transform hover:scale-105 transition-all duration-300"
        >
          <i class="fas fa-play-circle text-xl"></i> Watch Video
        </button>
      </div>
    </div>
  </div>
</section>

<!-- 🔽 Bottom Full-Width Highlight Section -->
<section
  class="relative w-full bg-cover bg-[center_30%] py-10 px-6 text-white"
  style="background-image: url('https://img.freepik.com/free-photo/young-people-celebrating-youth-day_23-2151715318.jpg?t=st=1752598520~exp=1752602120~hmac=3fcd02143344aed3e906271095a8b06690b811cfdb2a0e6b083c78d3ad8bb198&w=2000');"
>
  <!-- Gradient overlay -->
  <div
    class="absolute inset-0 bg-gradient-to-r from-[#2F80ED]/90 to-[#56CCF2]/90 z-0"
  >
  </div>

  <div
    class="relative z-10 flex flex-col md:flex-row justify-center items-center gap-14 md:gap-20"
  >
    <!-- Online Courses -->
    <div
      class="flex items-center gap-6 max-w-sm"
      data-aos="fade-up"
      data-aos-delay="100"
      data-aos-duration="800"
    >
      <i class="fas fa-book text-4xl md:text-5xl"></i>
      <div>
        <p class="text-xl md:text-2xl font-semibold">2,769 online courses</p>
        <p class="text-base md:text-lg text-white/90">
          Explore a variety of fresh topics
        </p>
      </div>
    </div>

    <!-- Expert Instruction -->
    <div
      class="flex items-center gap-6 max-w-sm"
      data-aos="fade-up"
      data-aos-delay="300"
      data-aos-duration="800"
    >
      <i class="fas fa-chalkboard-teacher text-4xl md:text-5xl"></i>
      <div>
        <p class="text-xl md:text-2xl font-semibold">Expert instruction</p>
        <p class="text-base md:text-lg text-white/90">
          Find the right instructor for you
        </p>
      </div>
    </div>

    <!-- Lifetime Access -->
    <div
      class="flex items-center gap-6 max-w-sm"
      data-aos="fade-up"
      data-aos-delay="500"
      data-aos-duration="800"
    >
      <i class="fas fa-infinity text-4xl md:text-5xl"></i>
      <div>
        <p class="text-xl md:text-2xl font-semibold">Lifetime access</p>
        <p class="text-base md:text-lg text-white/90">
          Learn on your schedule and time
        </p>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  const menuBtn = document.getElementById("menuBtn");
  const sidebar = document.getElementById("sidebar");
  const closeBtn = document.getElementById("closeBtn");
  const overlay = document.getElementById("overlay");

  menuBtn.addEventListener("click", () => {
    sidebar.classList.remove("translate-x-full");
    overlay.classList.remove("hidden");
  });

  closeBtn.addEventListener("click", () => {
    sidebar.classList.add("translate-x-full");
    overlay.classList.add("hidden");
  });

  overlay.addEventListener("click", () => {
    sidebar.classList.add("translate-x-full");
    overlay.classList.add("hidden");
  });
</script>

<!-- 🔁 Carousel Script -->
<script>
  const slides = document.querySelectorAll(".carousel-slide");
  let current = 0;
  setInterval(() => {
    slides[current].classList.remove("active");
    current = (current + 1) % slides.length;
    slides[current].classList.add("active");
  }, 4000);
</script>

<!-- Hero Animation Script -->
<script is:inline>
  document.addEventListener("DOMContentLoaded", function () {
    // Trigger hero animations after a brief delay
    setTimeout(() => {
      document.querySelector(".hero-title").classList.add("animate-in");
    }, 300);

    setTimeout(() => {
      document.querySelector(".hero-subtitle").classList.add("animate-in");
    }, 600);

    setTimeout(() => {
      document.querySelector(".hero-buttons").classList.add("animate-in");
    }, 900);
  });
</script>

<style>
  /* Hero Animation Styles */
  .hero-title,
  .hero-subtitle,
  .hero-buttons {
    opacity: 0;
    transform: translateY(50px);
    transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .hero-title.animate-in,
  .hero-subtitle.animate-in,
  .hero-buttons.animate-in {
    opacity: 1;
    transform: translateY(0);
  }

  /* Zoom animation for each slide */
  @keyframes slideZoom {
    0% {
      transform: scale(1.1);
    }
    100% {
      transform: scale(1);
    }
  }

  .carousel-slide.active {
    animation: slideZoom 4s ease-out;
  }

  /* Button hover effects */
  .hero-buttons button {
    transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .hero-buttons button:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
  }

  /* Navbar - remove animation */
  nav {
    /* No animation */
  }

  /* Top bar - remove animation */
  .bg-[#004BB8] {
    /* No animation */
  }

  /* Carousel slide transitions */
  .carousel-slide {
    transition: opacity 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .carousel-slide.active {
    opacity: 1;
  }

  /* Responsive animations */
  @media (max-width: 768px) {
    .hero-title,
    .hero-subtitle,
    .hero-buttons {
      transform: translateY(30px);
    }
  }
</style>
